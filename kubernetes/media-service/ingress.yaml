apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: media-service-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx

  rules:
    - host: media.veleda.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: media-service
                port:
                  number: 80